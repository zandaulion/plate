<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Kept for the explicit, user-controlled Open Food Facts lookup and
         Firebase/Gemini analysis. All diary data and packaged app assets stay
         local; no broad storage permission is needed. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="Bitey"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Plate"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".BarcodeScanActivity"
            android:exported="false"
            android:screenOrientation="portrait" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
